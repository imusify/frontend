import { NeobalancePipe } from './neobalance.pipe';

describe('NeobalancePipe', () => {
  it('create an instance', () => {
    const pipe = new NeobalancePipe();
    expect(pipe).toBeTruthy();
  });
});
